export interface DatePickerValue {
    selectedDate?: Date;
    selectedDates?: Date[];
    startDate?: Date;
    endDate?: Date;
    
}

export type DatePickerType = "single" | "multiple" | "range"

